import React, { useState } from 'react';

const useUserData = () => {
    const [userData,setUserData]=useState([])
    return (
        <div>
            
        </div>
    );
};

export default useUserData;